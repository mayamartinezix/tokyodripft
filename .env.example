# --- Database (points at the DB laptop's LAN IP once deployed) ---
POSTGRES_HOST=localhost
POSTGRES_PORT=5432
POSTGRES_DB=resale
POSTGRES_USER=resale
POSTGRES_PASSWORD=changeme

# --- Notifications (ntfy instance on the DB laptop) ---
NTFY_URL=http://localhost/resale-alerts

# --- BDroppy / BrandsDistribution (fill in once your reseller agreement is approved) ---
BDROPPY_FEED_URL=
BDROPPY_API_KEY=

# --- Mercari Japan (leave conservative until license/visa question is resolved) ---
MERCARI_JP_SEARCH_TERMS=
MERCARI_JP_MAX_REQUESTS_PER_RUN=10
MERCARI_JP_DELAY_SECONDS=5

# --- Pricing ---
TARGET_MIN_MARGIN_PCT=0.20

# --- Scheduler ---
RUN_INTERVAL_MINUTES=60
